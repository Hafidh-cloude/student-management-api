[project]
name = "student-management-api"
version = "0.1.0"
description = "REST API for managing student data"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "sqlalchemy>=2.0.52",
    "uvicorn>=0.52.4",
]
